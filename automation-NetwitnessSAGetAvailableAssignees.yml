comment: Returns the available NetWitness SA users to be assigned to incidents.
commonfields:
  id: NetwitnessSAGetAvailableAssignees
  version: -1
contentitemexportablefields:
  contentitemfields:
    definitionid: ""
    fromServerVersion: 5.0.0
    itemVersion: 1.0.12
    packID: RsaNetwitnessSecurityAnalytics
    packName: RSA NetWitness Security Analytics
    packPropagationLabels:
    - all
    prevname: ""
    propagationLabels: []
    toServerVersion: ""
dependson:
  must:
  - netwitness-im-get-available-assignees
dockerimage: demisto/python3:3.10.13.83255
engineinfo: {}
mainengineinfo: {}
name: NetwitnessSAGetAvailableAssignees
pswd: ""
runas: DBotWeakRole
runonce: false
script: |
  register_module_line('NetwitnessSAGetAvailableAssignees', 'start', __line__())
  ### pack version: 1.0.12




  def netwitness_im_get_available_assignees():
      resp = demisto.executeCommand("netwitness-im-get-available-assignees", demisto.args())

      if isError(resp[0]):
          result = resp
      else:
          data = demisto.get(resp[0], "Contents.availableAssignees")
          if data:
              data = data if isinstance(data, list) else [data]
              data = [{k: formatCell(row[k]) for k in row} for row in data]
              result = {"ContentsFormat": formats["table"], "Type": entryTypes["note"], "Contents": data}
          else:
              result = "No results."
      return_results(result)


  def main():  # pragma: no cover
      try:
          netwitness_im_get_available_assignees()
      except Exception as e:
          err_msg = f'Encountered an error while running the script: [{e}]'
          return_error(err_msg, error=e)


  if __name__ in ('__main__', '__builtin__', 'builtins'):
      main()

  register_module_line('NetwitnessSAGetAvailableAssignees', 'end', __line__())
scripttarget: 0
subtype: python3
system: true
tags:
- RSA NetWitness Security Analytics
type: python
