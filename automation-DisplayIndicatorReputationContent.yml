comment: Display the indicator context object in markdown format in a dynamic section
  layout
commonfields:
  id: DisplayIndicatorReputationContent
  version: -1
contentitemexportablefields:
  contentitemfields:
    definitionid: ""
    fromServerVersion: 6.0.0
    itemVersion: 1.1.4
    packID: CommunityCommonScripts
    packName: Community Common Scripts
    packPropagationLabels:
    - all
    prevname: ""
    propagationLabels: []
    toServerVersion: ""
dockerimage: demisto/python3:3.10.12.63474
enabled: true
engineinfo: {}
mainengineinfo: {}
name: DisplayIndicatorReputationContent
pswd: ""
runas: DBotWeakRole
runonce: false
script: |
  register_module_line('DisplayIndicatorReputationContent', 'start', __line__())
  ### pack version: 1.1.4


  try:
      scores = demisto.args()["cache"]["scores"]
  except Exception:
      demisto.results("No scores cached")
      sys.exit(0)

  md = ""

  for k, v in scores.items():
      md += v["content"]
      md += "\r\n\r\n"


  entry = {
      'Type': entryTypes["note"],
      'Contents': md,
      'ContentsFormat': formats['markdown'],
      'HumanReadable': md,
      'ReadableContentsFormat': formats['markdown']
  }

  demisto.results(entry)

  register_module_line('DisplayIndicatorReputationContent', 'end', __line__())
scripttarget: 0
subtype: python3
system: true
tags:
- dynamic-indicator-section
type: python
